Ext.apply(ms2Extend.combo, {

});



/*Ext.apply(ms2Extend.window, {
    record: Ext.define(null, {
        extend : abstractModule.window.record,
        constructor: function (config) {
            config = config || {};
            Ext.applyIf(config, {
                width: config.width || 800
            });
            this.callParent(arguments);
        },
    })
});*/